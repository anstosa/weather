{
  "name": "@weather/forecast-adjustment",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build --workspace @weather/domain && tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "typecheck": "npm run build --workspace @weather/domain && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@weather/domain": "0.0.0"
  }
}
