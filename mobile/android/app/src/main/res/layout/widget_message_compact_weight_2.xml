<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bedtime_root"
    android:layout_width="0dp"
    android:layout_height="match_parent"
    android:layout_weight="2"
    android:importantForAccessibility="yes">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <TextView
            android:id="@+id/bedtime_hour"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="3dp"
            android:layout_marginTop="0dp"
            android:fontFamily="@font/google_sans_regular"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:importantForAccessibility="no"
            android:textColor="@color/widget_foreground"
            android:textSize="10sp" />
        <!-- center the illustration in the space between label and temperature -->
        <FrameLayout
            android:id="@+id/bedtime_icon_space"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">
            <!-- fill the available gap without fixed icon caps or distorted artwork -->
            <ImageView
                android:id="@+id/bedtime_icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:importantForAccessibility="no"
                android:scaleType="fitCenter" />
        </FrameLayout>
        <TextView
            android:id="@+id/bedtime_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="3dp"
            android:layout_marginBottom="0dp"
            android:fontFamily="@font/google_sans_bold"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:importantForAccessibility="no"
            android:textColor="@color/widget_foreground"
            android:textSize="16sp"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
