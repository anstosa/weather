{
  "name": "@weather/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build --workspace @weather/domain && npm run build --workspace @weather/database && npm run build --workspace @weather/providers && npm run build && node dist/worker.js",
    "test": "npm run build --workspace @weather/domain && npm run build --workspace @weather/database && npm run build --workspace @weather/providers && npm run build && node --test test/*.test.mjs",
    "test:integration": "npm run build --workspace @weather/domain && npm run build --workspace @weather/database && npm run build --workspace @weather/providers && npm run build && node --test --test-concurrency=1 test/*.integration.test.mjs",
    "weather:backfill": "npm run build --workspace @weather/domain && npm run build --workspace @weather/database && npm run build --workspace @weather/providers && npm run build && node dist/backfill-cli.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@weather/database": "0.0.0",
    "@weather/domain": "0.0.0",
    "@weather/providers": "0.0.0"
  }
}
